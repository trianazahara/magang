<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Sidebar</title>
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
   <style>
       .sidebar {
           width: 250px;
           background: white;
           min-height: 100vh;
           padding: 20px;
           box-shadow: 2px 0 5px rgba(0,0,0,0.1);
       }

       .brand {
           display: flex;
           align-items: center;
           gap: 10px;
           margin-bottom: 30px;
       }

       .brand img {
           width: 40px;
       }

       .brand-text {
           color: #0066cc;
           font-weight: 500;
           line-height: 1.2;
       }

       .nav-list {
           list-style: none;
           padding: 0;
           margin: 0;
       }

       .nav-item {
           margin-bottom: 5px;
       }

       .nav-link {
           display: flex;
           align-items: center;
           padding: 12px 15px;
           color: #666;
           text-decoration: none;
           border-radius: 5px;
           transition: all 0.3s;
       }

       .nav-link:hover {
           background: #f0f7ff;
           color: #0066cc;
       }

       .nav-link.active {
           background: #f0f7ff;
           color: #0066cc;
       }

       .nav-link i {
           margin-right: 10px;
           font-size: 20px;
       }

       .logout {
           position: fixed;
           bottom: 20px;
           width: 210px;
       }
   </style>
</head>
<body>

<div class="sidebar">
   <!-- Brand -->
   <div class="brand">
       <img src="/img/logo.png" alt="Logo">
       <div class="brand-text">
           <div>Magang</div>
           <div>Manager</div>
       </div>
   </div>

   <!-- Navigation -->
   <ul class="nav-list">
       <li class="nav-item">
           <a href="/dashboard" class="nav-link <%= path === '/dashboard' ? 'active' : '' %>">
               <i class="bi bi-house-door"></i>
               <span>Dashboard</span>
           </a>
       </li>
       <li class="nav-item">
           <a href="/manajemen-data" class="nav-link <%= path === '/manajemen-data' ? 'active' : '' %>">
               <i class="bi bi-database"></i>
               <span>Manajemen Data</span>
           </a>
       </li>
       <li class="nav-item">
           <a href="/riwayat-data" class="nav-link <%= path === '/riwayat-data' ? 'active' : '' %>">
               <i class="bi bi-clock-history"></i>
               <span>Riwayat Data Magang</span>
           </a>
       </li>
       <li class="nav-item">
           <a href="/manajemen-admin" class="nav-link <%= path === '/manajemen-admin' ? 'active' : '' %>">
               <i class="bi bi-people"></i>
               <span>Manajemen Admin</span>
           </a>
       </li>
       <li class="nav-item">
           <a href="/pengaturan" class="nav-link <%= path === '/pengaturan' ? 'active' : '' %>">
               <i class="bi bi-gear"></i>
               <span>Pengaturan</span>
           </a>
       </li>
   </ul>

   <!-- Logout -->
   <div class="logout">
       <a href="/logout" class="nav-link">
           <i class="bi bi-box-arrow-right"></i>
           <span>Logout</span>
       </a>
   </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>